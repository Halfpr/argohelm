apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-appsc-config
  namespace: {{ .Release.Namespace }}
data:
  {{- range $key, $value := .Values.configMap.data }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
